from feature import home_feature

if __name__ == "__main__":
    """
    This is the main entry point of the library management system.
    It calls the go_to_home() function from the home_feature module to start the application.
    """
    home_feature.go_to_home()
